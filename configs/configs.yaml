# Configurations to run YOLOv5

weights: yolov5s.pt # should change to custom weights
source:

conf_thres: 0.6
device: # should be HoopTracker gcloud CPU

save_txt: True # save results to *.txt
save_conf: True # save confidences in --save-txt labels
save_crop: True # save cropped prediction boxes

classes: 0, 32

project: # save location
name: # save file name